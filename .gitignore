(diff_report.txt is a text file that appears to be source/config content, not a build artifact, temp file, or dependency - following rule 6 where only source/config files should result in empty output)